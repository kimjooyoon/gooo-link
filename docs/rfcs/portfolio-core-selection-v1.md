# Portfolio-aware core improvement selection v1

## Decision corrected

The historical selector returned `RERUN_CONSUMER_ADOPTIONS` after the required external adoptions were already complete. `gooo-link v0.7.0-dev` closes the generator-consumer observation at 2/2 independent repositories, 10/10 immutable evidence assets, 24/24 generated cells, and 12/12 verification cells. Repeating adoption is therefore a refuted next operation.

Portfolio promotion is not evidence for a specific core language primitive. This selector preserves that boundary instead of choosing one of the open experiments by title or apparent usefulness.

## Exact normal result

- meta cells: 5/12 CLOSED and 7/12 UNKNOWN;
- direct missing: 2;
- dependency blocked: 5;
- external consumers: 2/2;
- core primitive candidates with cross-consumer evidence: 0/1;
- open experimental pull requests observed: 29;
- experiment-to-primitive mappings: 0/29;
- merge-eligible experimental pull requests: 0/1;
- automatic merge allowed: false;
- cross-project required gates: 0;
- source repository writes: 0;
- local test executions: 0.

The one next operation is `OBSERVE_CROSS_CONSUMER_PRIMITIVE_NEED`. It asks independent products to expose a repeated missing language meaning before any core experiment is selected.

## Resolution

Missing primitive identity and missing PR mapping are separate DIRECT_MISSING facts. Semantic invariant, version lock, selected implementation PR, and final selection remain DEPENDENCY_BLOCKED. A portfolio report that escalates declarative generation evidence into core-language authority is REFUTED. A historical selector that proposes direct core implementation without cross-consumer evidence is also REFUTED.

The 29 experiments remain public research. They are neither rejected nor merge-eligible. Their titles, branches, and green checks are not substitutes for a mapping from one repeated consumer need to one primitive identity, invariant, version lock, and implementation PR.

## Non-bottleneck boundary

The selector reads a pinned `gooo-link v0.7.0-dev` release and a read-only GitHub PR snapshot. It does not make consumer releases depend on this selection, does not modify a consumer or core repository, and does not turn UNKNOWN into a core implementation mandate.
